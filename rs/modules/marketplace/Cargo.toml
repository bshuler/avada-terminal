[package]
name = "avada-marketplace"
version = "0.2.0"
edition = "2021"
rust-version = "1.89"
description = "Marketplace rail for Avada Terminal: search, install, enable, disable and remove modules"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "avada-marketplace"
path = "src/main.rs"

[dependencies]
avada-module-sdk = { path = "../../crates/module-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
libc = "0.2"

[lints.rust]
unsafe_code = "deny"
missing_docs = "warn"

[lints.clippy]
all = "warn"
