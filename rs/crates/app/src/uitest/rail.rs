//! Track H4: rail entry clicks reach Rust with the entry's string id.
//! Owned by the H4 track; `super::*` brings the harness helpers (`ui`, `window`,
//! `click`, `by_label`, `by_id`, ...) into scope.
#![allow(unused_imports)]

use super::*;
