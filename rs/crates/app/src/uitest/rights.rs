//! Track H2: the Preferences rights page rows and the ask-toast.
//! Owned by the H2 track; `super::*` brings the harness helpers (`ui`, `window`,
//! `click`, `by_label`, `by_id`, ...) into scope.
#![allow(unused_imports)]

use super::*;
