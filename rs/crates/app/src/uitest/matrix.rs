//! Track V5: docs/feature-test-matrix.md lists every Command and callback.
//! Owned by the V5 track; `super::*` brings the harness helpers (`ui`, `window`,
//! `click`, `by_label`, `by_id`, ...) into scope.
#![allow(unused_imports)]

use super::*;
