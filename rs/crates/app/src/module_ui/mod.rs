//! Module UI (docs/modules-fanout-plan.md, tracks H1/H4): the placeholder pane a module's
//! slot shows while the module is crashed, disabled or not installed, and the tier-1
//! renderer. Owned by the H1 track in Wave 1.

pub mod doc;
pub mod grid;
pub mod image;
pub mod placeholder;
pub mod rows;
