//! Module host (docs/modules-fanout-plan.md, track H1): spawn, transport, handshake,
//! per-module tokens, supervision, registry. Owned by the H1 track; the orchestrator
//! only declared the module.
