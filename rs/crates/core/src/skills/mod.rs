//! Skills materializer: writes the `SKILL.md` files a module ships into every agent tool
//! the project uses (track F4 of `docs/modules-fanout-plan.md`). Replaces the Hyperpane
//! materializer in `crate::hyperpane`. Pre-declared mount; the track owns this directory.
