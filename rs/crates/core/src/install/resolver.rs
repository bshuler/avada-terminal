//! Dependency resolution (docs/modules-fanout-plan.md §2 "Manifest, discovery,
//! dependencies", track G6): identity deps and extension-point `provides`/`requires`
//! by shape and semver on `pubgrub`, provider defaults, side-by-side versions and
//! workspace pin conflicts. Scaffolded by the orchestrator; track G6 fills it.
