//! Module marketplace: discovery by GitHub topic, source build with `rustup`, install /
//! enable / disable through [`crate::install::InstallStore`]. Track F2 of
//! `docs/modules-fanout-plan.md` fills this directory; the mount is pre-declared so the
//! track never edits `lib.rs`.
